version: '2'
services:
  refget_proxy:

    build: 
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    container_name: refget_proxy_api
    volumes:
      - ./app/:/app/
    ports:
      - 8083:8083
